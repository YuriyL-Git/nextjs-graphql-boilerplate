{
  "name": "Remote Schema",
  "schemaPath": "./src/generated/schema.graphql",
  "dotEnvPath": ".env",
  "extensions": {
    "endpoints": {
      "Remote SWAPI GraphQL Endpoint": {
        "url": "${env:HOST_NAME}api/graphql",
        "headers": {
          "user-agent": "JS GraphQL"
        },
        "introspect": false
      }
    }
  }
}
